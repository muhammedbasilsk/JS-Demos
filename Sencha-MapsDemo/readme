Uses xtype mapviews internally uses Leaflets.
When double click on  tweet list the corresponding location is highlighted in the map.
Each location corresponding to the tweet list have markers and tooltips.
